import 'package:flutter/material.dart';
class updatenewspage extends StatelessWidget {
  const updatenewspage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Coming soon'),),
    );
  }
}